requests to 
	/public/token  ( to get JWT )

	/public/welcome

are without authorization i.e. you don't have to pass 
Bearer <JWT token generated>

request to 
	/hello

you must pass JWT generated here

this is because we've given

http.csrf(csrf -> csrf.disable())
         .authorizeHttpRequests(auth -> auth
             .requestMatchers("/public/**").permitAll()
             .anyRequest().authenticated()

