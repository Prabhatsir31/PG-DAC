// Only the filename — no Images/ prefix, no .jpg added later

import React from "react";
function Getmimg(i) { 
    const imgurl = ['img', 'img1'];
    return imgurl[i];
  }
  export default Getmimg;
  